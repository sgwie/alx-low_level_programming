#include "main.h"
/**
 *_abs - Computes absolute number
 *
 *@a: Number to return absolute value
 *
 *Return: Absolute value
 */
int _abs(int a)
{
if (a >= 0)
{
return (a);
}
else
{
return (a * -1);
}
}
