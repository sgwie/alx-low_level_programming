#include "main.h"

/**
 *print_number - Prints integer number
 *
 *@n: number to be printed
 *
 *Return:void
 */

void print_number(int n)
{
unsigned int a;
x = a;
if (a < 0)
{
_putchar(45);
x = -n;
}
if (x / 10)
print_number(x / 10);
_putchar((x % 10) + '0');
}
